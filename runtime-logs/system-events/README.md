## `runtime-logs/system-events/`：系统事件日志

本目录用于存放系统事件日志文件（建议扩展名为 `.log`），记录与模型调用无关但对运行状态有意义的事件，例如：

- 主 Agent 或子 Agent 接管某个 change-id 的时间点；
- 任务启动、完成、取消、重试等关键节点；
- 模型调用失败与降级重试事件；
- 运行时异常的脱敏描述。

> 运行期产物（`.log` 文件）通过上级目录的 `.gitignore` 忽略，不纳入版本控制。

推荐使用简单易读的文本格式（见 `runtime-logs/README.md` 示例）。

