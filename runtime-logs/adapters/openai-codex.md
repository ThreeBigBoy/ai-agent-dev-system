## OpenAI 兼容 / Codex 场景下的运行日志采集（示意草案）

对于直接调用 OpenAI 兼容 API（包括 Codex 或后续模型）的场景：

- 可使用响应中的 `usage` 字段获取精确的 tokens 消耗；
- 通过 HTTP 客户端或中间层记录请求耗时；
- 将调用的 `model`、`usage`、状态码等信息转换为 `model-calls/*.jsonl` 记录；
- 必须在应用层剥离或脱敏 Prompt 与回复正文，避免原始业务数据进入日志。

如需集中存储或进一步分析，应在后续单独的变更中引入相应的后端服务与安全策略。

