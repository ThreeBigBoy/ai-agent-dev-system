# 🚀 新用户快速开始

适用于第一次从 Git 下载 `ai-agent-dev-system` 的用户。

---

## 📋 这份文档解决什么问题

帮助你在最短路径内完成：

1. 了解仓库结构与规则优先级
2. 选择宿主
3. 按宿主完成初始化配置
4. 做首次 GUI 走查

---

## 📖 第一步先读什么

### 建议阅读顺序

| 序号 | 文档 | 路径 |
|:----:|------|------|
| 1 | **README.md** | `ai-agent-dev-system/README.md` |
| 2 | **OpenSpec.md** | `ai-agent-dev-system/OpenSpec.md` |
| 3 | **AGENTS.md** | `ai-agent-dev-system/AGENTS.md` |

### 补充说明

> 💡 **提示**：
> - 与重构背景、内部验收、设计推演相关的维护资料默认不作为新用户入口公开依赖。
> - 新用户只需完成本页与宿主初始化手册中的步骤，即可正常开始使用。
> - 规则文件（尤其是 `global-rules/projects-rules-for-agent.md`）已经按 simple/heavy 渐进加载与瘦身设计：简单任务下宿主与主 Agent 不会一上来就加载全部规则，而是优先使用入口规则与必要的 memory，只有在涉及变更入口/接口/数据结构/安全边界等重规则场景时，才会完整加载 global-rules 与 `agents/*.md`。

---

## 🎯 先选宿主

### 3.1 白名单宿主 ✅

| 宿主 | 策略 |
|------|------|
| **Cursor 官方** | 主 Agent 与子 Agent 都优先使用宿主内置模型 |
| **VS Code 官方 / GitHub Copilot** | 宿主内置模型异常或不可用时，再降级到个人 API 模型链路 |

### 3.2 第三方宿主 ⚡

| 宿主 | 策略 |
|------|------|
| **Continue** | 主 Agent 优先使用宿主内置模型；子 Agent / runtime backend 直接走个人 API 模型链路 |
| **OpenAI-Codex** | 同上 |

---

## 🔌 各宿主初始化入口

### Cursor

| 项目 | 内容 |
|------|------|
| **手册** | [Cursor 从0开始初始化配置SOP与GUI走查清单.md](platform-adapters/cursor/从0开始初始化配置SOP与GUI走查清单.md) |
| **关键前提** | 1. 打开仓库根目录<br>2. 配好 `~/.cursor/mcp.json`<br>3. 启用本地反馈插件 |

### VS Code

| 项目 | 内容 |
|------|------|
| **手册** | [VS Code 从0开始初始化配置SOP与GUI走查清单.md](platform-adapters/vscode/从0开始初始化配置SOP与GUI走查清单.md) |
| **关键前提** | 1. 打开仓库根目录<br>2. 根 `AGENTS.md` 与 `.github/agents/*.agent.md` 可见 |

### Continue

| 项目 | 内容 |
|------|------|
| **手册** | [Continue 从0开始初始化配置SOP与GUI走查清单.md](platform-adapters/generic/continue-从0开始初始化配置SOP与GUI走查清单.md) |
| **关键前提** | 1. 准备个人 API 凭据<br>2. runtime 传入 `AGENT_HOST_TYPE=continue` |

### OpenAI-Codex

| 项目 | 内容 |
|------|------|
| **手册** | [OpenAI-Codex 从0开始初始化配置SOP与GUI走查清单.md](platform-adapters/generic/openai-codex-从0开始初始化配置SOP与GUI走查清单.md) |
| **关键前提** | 1. 准备个人 API 凭据<br>2. runtime 传入 `AGENT_HOST_TYPE=openai-codex` |

---

## ⚙️ 通用初始化步骤

无论使用哪个宿主，通常都需要：

1. 克隆仓库并打开根目录
2. 安装 Python 依赖（见下方 5.1）
3. 准备 `agent_team_project/.env`
4. 确认 `agent_team_project/runtime_config.json`
5. 验证主 Agent 身份入口
6. 验证决策文件与反馈文件链路

---

## 🏗️ 5.0 新管线运行架构（LangGraph 独立后端）

从 V2.6 起，系统采用 **LangGraph 独立后端** 作为默认执行管线：

```
┌─────────────────────────────────────────────────────────────┐
│  🖥️  Cursor / VS Code  宿主层                                │
│  ┌─────────────────┐      ┌─────────────────────────────┐   │
│  │   👤 用户消息    │─────▶│   🔍 扩展检测                 │   │
│  │                 │      │   change-id + 关键词         │   │
│  └─────────────────┘      └─────────────────────────────┘   │
│                              │                              │
│                              ▼                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  🔌 MCP 工具 / HTTP 调用                                 ││
│  │     POST /run {change_id, task_range?}                  ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP / JSON
┌─────────────────────────────────────────────────────────────┐
│  ⚙️  LangGraph 独立后端 (FastAPI)                            │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  🔄 StateGraph: parse_tasks → dispatch → collect        ││
│  │                                                         ││
│  │  ┌──────────────┐    ┌──────────┐    ┌──────────────┐   ││
│  │  │ 📋 parse    │───▶│ ⚡ dispatch│───▶│ ✅ collect   │   ││
│  │  │    tasks    │    │            │    │    feedback │   ││
│  │  └──────────────┘    └──────────┘    └──────────────┘   ││
│  │       │                    │                  │         ││
│  │       ▼                    ▼                  ▼         ││
│  │  ┌─────────────────────────────────────────────────────┐││
│  │  │ 💾 检查点 (MemorySaver / Redis)                      │││
│  │  │ state: {change_id, decision, results, feedback}     │││
│  │  └─────────────────────────────────────────────────────┐││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ 调用 7 个 executor
┌─────────────────────────────────────────────────────────────┐
│  🤖 API 模型层 (SiliconFlow / OpenAI 等)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│  │ 产品经理 │ │ 架构师   │ │ 前端    │ │ 后端    │  + 3 更多    │
│  │  Agent  │ │  Agent  │ │  Agent  │ │  Agent  │            │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘            │
└─────────────────────────────────────────────────────────────┘
```

### 核心特点

| 特点 | 说明 |
|------|------|
| **状态图驱动** | LangGraph `compile()` 后代码强制流转，100% 无跳过 |
| **检查点持久化** | 支持断点续跑（`/resume` 接口），状态不丢失 |
| **多业务项目支持** | 通过 `LANGGRAPH_WORKSPACE_PROJECTS` 配置多个项目，按 change_id 自动解析 |
| **本仓优先** | 后端始终先查本仓 `openspec/changes/`，再试业务项目 |
| **独立留痕** | 执行记录写入 `runtime-logs/langgraph-runs/`，不依赖迭代日志或 design/documents |

---

## 📦 5.1 安装 Python 依赖（含 LangGraph 前置环境）

> ⚠️ **前置条件**：新机器 / 新环境 / 依赖升级后，必须**手动执行一次**本步骤，系统不会自动安装。

---

### 方案 A：一键安装（推荐）

**macOS / Linux**：

```bash
cd agent_team_project
bash setup-langgraph-env.sh
```

**Windows**：

```cmd
setup-langgraph-env.bat
```

**脚本完成后的提示**：
- ✅ 检查 Python 3.9+
- ✅ 创建 `.venv` 虚拟环境
- ✅ 安装 8 个必装包并自检导入
- ✅ 按终端提示激活环境：`source .venv/bin/activate`

---

### 方案 B：手动安装

若一键脚本遇到问题，或需要更细粒度控制：

```bash
# 1. 进入目录
cd agent_team_project

# 2. 创建虚拟环境
python3 -m venv .venv

# 3. 激活虚拟环境
source .venv/bin/activate        # macOS/Linux
# .venv\Scripts\activate           # Windows

# 4. 安装依赖
pip install -r requirements.txt

# 5. 验证安装
python3 -c "import fastapi, langgraph, langchain_openai; print('全部依赖已就绪')"
```

---

### 必装依赖清单

| 包 | 最低版本 | 用途说明 |
|----|:--------:|----------|
| langgraph | ≥ 0.2.0 | LangGraph 状态图框架 |
| fastapi | ≥ 0.110.0 | FastAPI 服务框架 |
| uvicorn[standard] | ≥ 0.27.0 | ASGI 服务器 |
| pydantic | ≥ 2.0 | 数据验证 |
| slowapi | ≥ 0.1.0 | 限流保护 |
| python-multipart | ≥ 0.0.5 | 表单解析 |
| **langchain-core** | ≥ 0.3.0 | **【关键】executor 调用 LLM 必需** |
| **langchain-openai** | ≥ 0.2.0 | **【关键】executor 调用 LLM 必需** |

> ⚠️ **重要**：`langchain-core` 与 `langchain-openai` 是 executor 任务正常运行的**必要依赖**。未安装时，流程可跑但任务会报「未安装 langchain-openai」错误。

---

### 何时需要重新执行安装

| 场景 | 操作 |
|------|------|
| 新机器首次使用 | 必须执行方案 A 或 B |
| 依赖升级后 | 重新执行 `bash setup-langgraph-env.sh` 或 `pip install -r requirements.txt` |
| 报错「未安装 langchain-openai」 | 先执行本步骤，再**用同一 .venv 重启后端**（见 5.2 节） |

---

### 虚拟环境日常激活

后续每次使用 LangGraph 功能前，确保虚拟环境已激活：

```bash
cd agent_team_project
source .venv/bin/activate        # macOS/Linux
# .venv\Scripts\activate         # Windows
```

---

## ▶️ 5.2 启动 LangGraph 后端与 API 配置

> ⚠️ **前置检查**：确保已完成 5.1 节依赖安装，且 `.venv` 内已包含 `langchain-openai`。

---

### 步骤 1：配置 API 密钥

**新建 `.env` 文件**（已有则检查）：

```bash
cd agent_team_project
cp .env.example .env    # 复制模板
```

编辑 `.env`，填入以下必填项：

```bash
OPENAI_API_KEY=sk-your-key-here                # 必填
OPENAI_API_BASE_URL=https://api.xxx.com/v1     # 可选：第三方兼容端点
```

> 🔒 **安全提醒**：`.env` 文件已加入 `.gitignore`，**切勿提交到版本库**。

---

### 步骤 2：启动后端服务

后端服务运行在**本机某个终端进程**里，与 Cursor/VS Code 是否打开、工作区是否切换无关；该终端被关闭或进程被结束，后端即停止。

#### 何时需要启动

| 场景 | 说明 |
|------|------|
| **首次使用 / 新环境** | 本机第一次用 LangGraph 管线，或新机器/新 clone 后从未启动过后端 → 执行步骤 2 |
| **退出 IDE 后重新打开** | 关闭 IDE 时，其内的终端通常一并退出，后端进程会结束 → **需要重新执行步骤 2** |
| **重新打开工作区** | 若之前是关掉整个 IDE 再重新「打开文件夹」，之前跑后端的终端已不存在 → **需要执行步骤 2** |
| **关闭了「正在跑后端」的那个终端** | 后端随该终端一起退出 → **需要执行步骤 2** |
| **仅新开一个终端标签页** | 后端可能仍在别的标签页运行 → **先不必启动**，执行步骤 3 健康检查 |
| **修改了后端代码、依赖或 .env** | 要让新配置生效 → 结束旧进程后重新启动 |

#### 如何判断「要不要启动」

```bash
curl -s http://127.0.0.1:8000/health
```

- ✅ 返回 `{"status":"healthy",...}` → 后端已在运行，**无需再执行步骤 2**
- ❌ 提示连接失败或 `Connection refused` → 执行步骤 2
- ⚠️ 启动时报「address already in use」→ 多半是上次的后端仍在跑，**可不 kill、直接使用**，或按需 kill 后重新启动

---

#### 方案 A：一键启动脚本（推荐）

任选以下一种方式执行：

| 当前所在目录 | 操作方式 |
|--------------|----------|
| **方式 1**：在 **ai-agent-dev-system 仓库根** | 复制整段执行：`cd agent_team_project` → `bash start-langgraph-backend.sh` |
| **方式 2**：已在 **agent_team_project** 目录下 | 只执行：`bash start-langgraph-backend.sh` |
| **方式 3**：希望用 `./脚本` 直接执行 | 先 `chmod +x start-langgraph-backend.sh`，再执行：`./start-langgraph-backend.sh` |

**方式 1（从仓库根）**：
```bash
cd agent_team_project
bash start-langgraph-backend.sh
```

**方式 2（已在 agent_team_project 下）**：
```bash
bash start-langgraph-backend.sh
```

**方式 3（加可执行权限后直接运行）**：
```bash
cd agent_team_project    # 若已在此目录可省略
chmod +x start-langgraph-backend.sh
./start-langgraph-backend.sh
```

> 💡 若遇 `permission denied`，用方式 1 或方式 2 的 `bash start-langgraph-backend.sh` 即可，无需 chmod。

**脚本自动完成**：
1. ✅ 设置 `AGENT_TEAM_PROJECT_ROOT`（自动推断本仓根）
2. ✅ 启动前检查 `.venv` 内 `langchain-openai`、`langgraph` 是否已安装
3. ✅ 缺失时**提示安装命令并退出**，不会继续启动
4. ✅ 正常时启动 `uvicorn`，监听 `http://127.0.0.1:8000`

---

#### 方案 B：手动启动（备用）

```bash
# 1. 进入目录并激活虚拟环境
cd agent_team_project
source .venv/bin/activate        # macOS/Linux
# .venv\Scripts\activate         # Windows

# 2. 设置仓库根路径（必须）
export AGENT_TEAM_PROJECT_ROOT="/你的/ai-agent-dev-system 仓库根路径"

# 3. 启动服务
uvicorn langgraph_backend.server:app --host 127.0.0.1 --port 8000
```

---

### 步骤 3：验证后端就绪

#### 快速健康检查

```bash
curl -s http://127.0.0.1:8000/health
```

**预期输出**：
```json
{"status":"healthy","version":"..."}
```

#### 进一步验证（多种细分场景）

若需系统化检查「后端健康 + 环境变量 + MCP 配置 + /run 与留痕」是否全部正常，可使用本仓**一键自检脚本**：

| 能力 | 说明 |
|------|------|
| **细分场景** | 「只验证环境不跑任务」「完整验证」「指定本仓/业务 change-id 验证」「指定后端地址」等 |
| **详细步骤** | 见 [`scripts/check-langgraph-backend/README.md`](scripts/check-langgraph-backend/README.md) |
| **包含内容** | 「细分场景快速参考」表格、运行目录与 `AGENT_TEAM_PROJECT_ROOT` 持久化说明、自检项说明 |

> 💡 **建议**：先完成上述 `curl` 快速检查，确认后端已启动后，再按需查阅自检脚本 README 做完整验证。

---

### 常见问题与快速修复

| 现象 | 原因 | 修复步骤 |
|------|------|----------|
| 后端启动后 `/run` 返回「未安装 langchain-openai」 | `.venv` 缺少关键依赖 | 1. `bash setup-langgraph-env.sh` <br> 2. 用同一 `.venv` 重启后端 |
| `start-langgraph-backend.sh` 提示「缺少依赖」并退出 | 启动前检查失败 | 同上 |
| 找不到 `AGENT_TEAM_PROJECT_ROOT` | 环境变量未设置 | 手动启动时需 `export AGENT_TEAM_PROJECT_ROOT=...`；持久化见步骤 2 或 `scripts/check-langgraph-backend/README.md` |
| `echo $AGENT_TEAM_PROJECT_ROOT` 只显示到 `.../Documents/AI` 等 | 路径含空格，写入 shell 配置时未加引号 | 按 `scripts/check-langgraph-backend/README.md`「若之前写入失败」清理错误行后，用 `REPO_ROOT=$(pwd)` 方式重新写入 |
| `[Errno 48] address already in use`（绑定 8000 端口失败） | 本机 8000 端口已被占用 | 1. 查占用：`lsof -i :8000` <br> 2. 结束进程：`kill <PID>` <br> 3. 再执行 `bash start-langgraph-backend.sh` |
| 自检脚本第 2 项不通过 | 未在仓库根执行或未设置 `AGENT_TEAM_PROJECT_ROOT` | 在 **ai-agent-dev-system 仓库根**执行，并设置环境变量 |
| 想系统化验证环境、MCP 配置与留痕 | 需多种细分场景的验证命令 | 见 `scripts/check-langgraph-backend/README.md` 的「细分场景快速参考」 |

---

### 业务项目使用新管线（多项目配置）

如需在业务项目（如 Proj01ShopifyTheme）中使用 `run_langgraph`，在 `~/.cursor/mcp.json` 中配置 `LANGGRAPH_WORKSPACE_PROJECTS`：

```json
{
  "mcpServers": {
    "langgraph-backend": {
      "command": "python3",
      "args": [
        "/你的路径/ai-agent-dev-system/agent_team_project/langgraph_mcp_server.py"
      ],
      "env": {
        "AGENT_HOST_TYPE": "cursor",
        "LANGGRAPH_WORKSPACE_PROJECTS": "[{\"LANGGRAPH_PROJECT_KEY\":\"Proj01ShopifyTheme\",\"LANGGRAPH_WORKSPACE_ROOT\":\"/Users/xxx/Cursor Projects/Proj01ShopifyTheme\"},{\"LANGGRAPH_PROJECT_KEY\":\"test_bizproject\",\"LANGGRAPH_WORKSPACE_ROOT\":\"/Users/xxx/Cursor Projects/test_bizproject\"}]"
      }
    }
  }
}
```

**配置要点**：
- 后端**优先读本仓**（`ai-agent-dev-system/openspec/changes/`）
- 若本仓无该 `change_id`，按列表顺序尝试业务项目
- 首个匹配项目即命中，无需手动指定「当前项目」

**验证多项目与业务 change-id**：可运行一键自检脚本，在「完整验证 + 指定业务项目」等场景下检查留痕与目录配置，见 `scripts/check-langgraph-backend/README.md` 细分场景快速参考。

详见 `platform-adapters/cursor/mcp-setup.md` §6。

---

## ✅ 6. 首次验收看什么

重点检查：

1. ✅ 自己使用的宿主属于「白名单宿主」还是「第三方宿主」
2. ✅ `AGENT_HOST_TYPE` 是否传对
3. ✅ 决策文件 / 反馈文件是否生成
4. ✅ 主 Agent 是否按治理规则回答

如果以上 4 项都正常，说明首次初始化基本通过。

> 💡 更详细的内部联调验收资料属于维护资料，默认不作为新用户入口公开依赖。

---

## 📚 7. 进阶：规则 × SKILL × memory 的协同理解（可选）

### 三层架构

| 层级 | 文件 | 作用 |
|------|------|------|
| **规则层** | `OpenSpec.md`、`global-rules/*.md`、`agents/*.md`、`skills-rules-for-agent.md` | 定义「谁/何时/必须做什么」以及极薄的结论级 HOW |
| **技能层** | `skills/*/SKILL.md` | 描述「怎么一步步做」「产出物最低结构」 |
| **记忆层** | `memory/` | 存放多次迭代中沉淀的 patterns / anti-patterns / reflections / playbooks / preferences |

> 💡 **简单理解**：
> - **rules** 定义「该不该做、什么时候做、做到什么程度算达标」
> - **SKILL 和 memory** 定义「具体怎么做」

你可以先不深入记忆这部分，只要知道它们存在即可，后续当关心运行日志、长期记忆或规则演进时再按需查阅。

---

## 📝 8. 进阶：启用运行日志与长期记忆（可选）

### 运行日志（runtime-logs）

| 项目 | 说明 |
|------|------|
| **目录** | `runtime-logs/` |
| **README.md** | 字段说明与宿主适配概要 |
| **model-calls/** | 按日期分片的 JSON Lines 模型调用日志 |
| **system-events/** | 系统事件文本日志 |
| **脚本** | `scripts/runtime-logging/append_cursor_model_call.py` — 统一命令行接口，用于向 `model-calls/*.jsonl` 追加一条模型调用记录 |

### 长期记忆（memory）

| 项目 | 说明 |
|------|------|
| **目录** | `memory/` |
| **README.md** | 长期记忆库说明 |
| **schema.md** | frontmatter 字段规范 |
| **子目录** | `patterns/`、`anti-patterns/`、`preferences/`、`playbooks/`、`reflections/` |
| **脚本** | `scripts/memory/create_memory_entry.py` — 用于按类型创建新的长期记忆条目 |

> 💡 **提示**：对于新用户，不必一开始就启用上述能力；当你开始关心「模型调用成本、稳定性」或希望「跨 change-id 复用经验」时，再按各宿主的从 0 初始化手册底部的「进阶能力」小节接入即可。

---

## ❓ 9. 新用户最常见问题

### 9.1 为什么打开后主 Agent 不生效

通常是因为：
- ❌ 没打开仓库根目录
- ❌ 宿主入口文件没被加载
- ❌ 没重启 IDE

### 9.2 为什么 runtime 不执行

通常是因为：
- ❌ Python 依赖没装 → 在 `agent_team_project` 下执行 `bash setup-langgraph-env.sh`（见 5.1）
- ❌ `AGENT_TEAM_PROJECT_ROOT` 没传
- ❌ 第三方宿主忘了传 `AGENT_HOST_TYPE`
- ❌ `.env` 没配好

### 9.3 为什么有的宿主子 Agent 不走内置模型

这是**设计行为**：
- ✅ **Cursor / VS Code**（白名单宿主）：子 Agent 内置优先
- ⚡ **Continue / OpenAI-Codex**（第三方宿主）：子 Agent 直接走 API
