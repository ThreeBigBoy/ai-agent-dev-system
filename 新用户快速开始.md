# 新用户快速开始

## 1. 这份文档解决什么问题

适用于第一次从 Git 下载 `ai-agent-dev-system` 的用户。  
目标是帮助你在最短路径内完成：

1. 了解仓库结构与规则优先级
2. 选择宿主
3. 按宿主完成初始化配置
4. 做首次 GUI 走查

## 2. 第一步先读什么

建议阅读顺序：

1. [README.md]，文档路径在此项目根目录下/ai-agent-dev-system/README.md
2. [OpenSpec.md]，文档路径在此项目根目录下/ai-agent-dev-system/OpenSpec.md
3. [AGENTS.md]，文档路径在此项目根目录下/ai-agent-dev-system/AGENTS.md

补充说明：

- 与重构背景、内部验收、设计推演相关的维护资料默认不作为新用户入口公开依赖。  
- 新用户只需完成本页与宿主初始化手册中的步骤，即可正常开始使用。  
- 规则文件（尤其是 `global-rules/projects-rules-for-agent.md`）已经按 simple/heavy 渐进加载与瘦身设计：简单任务下宿主与主 Agent 不会一上来就加载全部规则，而是优先使用入口规则与必要的 memory，只有在涉及变更入口/接口/数据结构/安全边界等重规则场景时，才会完整加载 global-rules 与 `agents/*.md`。

## 3. 先选宿主

当前支持分两类：

### 3.1 白名单宿主

- Cursor 官方
- VS Code 官方 / GitHub Copilot

策略：

- 主 Agent 与子 Agent 都优先使用宿主内置模型
- 宿主内置模型异常或不可用时，再降级到个人 API 模型链路

### 3.2 第三方宿主

- Continue
- OpenAI-Codex

策略：

- 主 Agent 优先使用宿主内置模型
- 子 Agent / runtime backend 直接走个人 API 模型链路

## 4. 各宿主初始化入口

### 4.1 Cursor

- 手册：
  - [Cursor 从0开始初始化配置SOP与GUI走查清单.md]，文档路径在此项目根目录下/ai-agent-dev-system/platform-adapters/cursor/从0开始初始化配置SOP与GUI走查清单.md
- 关键前提：
  - 打开仓库根目录
  - 配好 `~/.cursor/mcp.json`
  - 启用本地反馈插件

### 4.2 VS Code

- 手册：
  - [VS Code 从0开始初始化配置SOP与GUI走查清单.md]，文档路径在此项目根目录下/ai-agent-dev-system/platform-adapters/vscode/从0开始初始化配置SOP与GUI走查清单.md
- 关键前提：
  - 打开仓库根目录
  - 根 `AGENTS.md` 与 `.github/agents/*.agent.md` 可见

### 4.3 Continue

- 手册：
  - [Continue 从0开始初始化配置SOP与GUI走查清单.md]，文档路径在此项目根目录下/ai-agent-dev-system/platform-adapters/generic/continue-从0开始初始化配置SOP与GUI走查清单.md
- 关键前提：
  - 准备个人 API 凭据
  - runtime 传入 `AGENT_HOST_TYPE=continue`

### 4.4 OpenAI-Codex

- 手册：
  - [OpenAI-Codex 从0开始初始化配置SOP与GUI走查清单.md]，文档路径在此项目根目录下/ai-agent-dev-system/platform-adapters/generic/openai-codex-从0开始初始化配置SOP与GUI走查清单.md
- 关键前提：
  - 准备个人 API 凭据
  - runtime 传入 `AGENT_HOST_TYPE=openai-codex`

## 5. 通用初始化步骤

无论使用哪个宿主，通常都需要：

1. 克隆仓库并打开根目录
2. 安装 Python 依赖（见下方 5.1）
3. 准备 `agent_team_project/.env`
4. 确认 `agent_team_project/runtime_config.json`
5. 验证主 Agent 身份入口
6. 验证决策文件与反馈文件链路

### 5.0 新管线运行架构（LangGraph 独立后端）

从 V2.6 起，系统采用 **LangGraph 独立后端** 作为默认执行管线，架构如下：

```
┌─────────────────────────────────────────────────────────────┐
│                      Cursor / VS Code                       │
│  ┌─────────────────┐      ┌─────────────────────────────┐  │
│  │   用户消息       │─────▶│   扩展检测 change-id + 关键词  │  │
│  └─────────────────┘      └─────────────────────────────┘  │
│                              │                              │
│                              ▼                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  MCP 工具 / HTTP 调用                                     ││
│  │  POST /run {change_id, task_range?}                    ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP / JSON
┌─────────────────────────────────────────────────────────────┐
│                  LangGraph 独立后端 (FastAPI)                │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  StateGraph: parse_tasks → dispatch → collect_feedback ││
│  │                                                         ││
│  │  ┌──────────────┐    ┌──────────┐    ┌──────────────┐  ││
│  │  │ parse_tasks  │───▶│ dispatch │───▶│ collect_fb   │  ││
│  │  │ (节点函数)    │    │ (节点函数)│    │ (节点函数)    │  ││
│  │  └──────────────┘    └──────────┘    └──────────────┘  ││
│  │       │                    │                  │         ││
│  │       ▼                    ▼                  ▼         ││
│  │  ┌─────────────────────────────────────────────────────┐││
│  │  │ 检查点 (MemorySaver / Redis)                       │││
│  │  │ state: {change_id, decision, results, feedback}    │││
│  │  └─────────────────────────────────────────────────────┐││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ 调用 7 个 executor
┌─────────────────────────────────────────────────────────────┐
│                    API 模型 (SiliconFlow等)                   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ 产品经理 │ │ 架构师   │ │ 前端工程师│ │ 后端工程师│ ...       │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
└─────────────────────────────────────────────────────────────┘
```

**核心特点**：
- **状态图驱动**：LangGraph `compile()` 后代码强制流转，100% 无跳过
- **检查点持久化**：支持断点续跑（`/resume` 接口），状态不丢失
- **多业务项目支持**：通过 `LANGGRAPH_WORKSPACE_PROJECTS` 配置多个项目，按 change_id 自动解析
- **本仓优先**：后端始终先查本仓 `openspec/changes/`，再试业务项目
- **独立留痕**：执行记录写入 `runtime-logs/langgraph-runs/`，不依赖迭代日志或 design/documents

### 5.1 安装 Python 依赖（含 LangGraph 前置环境）

**推荐**：使用 `agent_team_project` 目录下的一键安装 + 自检脚本，自动创建虚拟环境、安装必装包并做自检。

- **macOS / Linux**：在终端执行
  ```bash
  cd agent_team_project
  bash setup-langgraph-env.sh
  ```
- **Windows**：在 `agent_team_project` 目录下双击或在 cmd 中执行
  ```cmd
  setup-langgraph-env.bat
  ```

脚本会：检查 Python 3.9+、创建 `.venv`、安装下述 6 个必装包并自检导入是否成功。完成后按提示激活虚拟环境即可（如 `source .venv/bin/activate`）。

**依赖清单（必装 8 个，需 Python 3.9+）**

| 包 | 最低版本 |
|----|----------|
| langgraph | ≥ 0.2.0 |
| fastapi | ≥ 0.110.0 |
| uvicorn[standard] | ≥ 0.27.0 |
| pydantic | ≥ 2.0 |
| slowapi | ≥ 0.1.0 |
| python-multipart | ≥ 0.0.5 |
| langchain-core | ≥ 0.3.0 |
| langchain-openai | ≥ 0.2.0 |

后两个用于后端执行器（executor）调用 API 模型；未安装时流程可跑但任务结果会报「未安装 langchain-openai」。

**以后在新环境或新机器上**：在 `agent_team_project` 下执行 `bash setup-langgraph-env.sh`，或先 `source .venv/bin/activate` 再 `pip install -r requirements.txt`，即可从环境上解决「未安装 langchain-openai」问题。

**手动安装步骤**

1. 进入目录：`cd agent_team_project`
2. 创建虚拟环境：`python3 -m venv .venv`
3. 激活虚拟环境：
   - macOS/Linux：`source .venv/bin/activate`
   - Windows：`.venv\Scripts\activate`
4. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
   或逐包安装：`pip install "langgraph>=0.2.0" "fastapi>=0.110.0" "uvicorn[standard]>=0.27.0" "pydantic>=2.0" "slowapi>=0.1.0" "python-multipart>=0.0.5" "langchain-core>=0.3.0" "langchain-openai>=0.2.0"`
5. 验证：`python3 -c "import fastapi, langgraph, langchain_openai; print('全部依赖已就绪')"`

> **提示 1**：在需要运行或开发 LangGraph 相关功能时，请先激活虚拟环境（如 `cd agent_team_project` 后执行 `source .venv/bin/activate`（macOS/Linux）或 `.venv\Scripts\activate`（Windows））。
>
> **提示 2**：上述依赖用于 **LangGraph 独立后端**——即多 Agent 协同的编排与执行服务，由 Cursor 等 IDE 通过 MCP 或 HTTP 调用。激活虚拟环境后，可通过 `uvicorn langgraph_backend.server:app` 启动该后端服务（默认监听 http://localhost:8000）。**需先实现 `langgraph_backend` 模块**（即仓库中需存在 `agent_team_project/langgraph_backend/` 及其中的 `server.py` 等代码）；若该模块尚未落地，需等待或参与对应变更实现后再启动。

### 5.2 启动 LangGraph 后端与 API 配置

本机若已在 `agent_team_project` 下用 5.1 装好依赖（含 `langchain-openai`），**直接用该 .venv 启动后端即可正常跑 executor 任务**。

1. **配置 API 密钥（必须）**  
   在 `agent_team_project/.env` 或当前 shell 环境变量中配置：
   - `OPENAI_API_KEY`：必填，用于执行器调用 OpenAI 兼容 API。
   - `OPENAI_API_BASE_URL`：可选，使用第三方兼容端点时填写（如 `https://api.xxx.com/v1`）。
   - **本机若 `.env` 已存在且含 `OPENAI_API_KEY`，无需再改**；**新用户**可复制 `agent_team_project/.env.example` 为 `.env` 后填入自己的 key（勿将 `.env` 提交到版本库）。

2. **设置仓库根并启动服务**  
   ```bash
   cd agent_team_project
   source .venv/bin/activate   # Windows: .venv\Scripts\activate
   export AGENT_TEAM_PROJECT_ROOT="/你的/ai-agent-dev-system 仓库根路径"
   uvicorn langgraph_backend.server:app --host 127.0.0.1 --port 8000
   ```
   健康检查：`curl -s http://127.0.0.1:8000/health` 返回 `{"status":"healthy",...}` 即表示就绪。

3. **业务项目下使用新管线（run_langgraph）时**  
   在 **~/.cursor/mcp.json** 的 **langgraph-backend.env** 中配置 **`LANGGRAPH_WORKSPACE_PROJECTS`**（JSON 数组字符串如 `"[{\"LANGGRAPH_PROJECT_KEY\":\"k\",\"LANGGRAPH_WORKSPACE_ROOT\":\"/path\"},...]"` 或扁平串 `key|path:key2|path2`）即可，后端**优先读本仓**、再按 change_id 在列表中自动解析；仅在本仓迭代时不设或留空。详见 `platform-adapters/cursor/mcp-setup.md` §6。

   **多业务项目配置示例（JSON 数组格式）**：
   ```json
   "langgraph-backend": {
     "command": "python3",
     "args": [
       "/你的路径/ai-agent-dev-system/agent_team_project/langgraph_mcp_server.py"
     ],
     "env": {
       "LANGGRAPH_WORKSPACE_PROJECTS": "[{\"LANGGRAPH_PROJECT_KEY\":\"Proj01ShopifyTheme\",\"LANGGRAPH_WORKSPACE_ROOT\":\"/Users/xxx/Cursor Projects/Proj01ShopifyTheme\"},{\"LANGGRAPH_PROJECT_KEY\":\"test_bizproject\",\"LANGGRAPH_WORKSPACE_ROOT\":\"/Users/xxx/Cursor Projects/test_bizproject\"}]"
     }
   }
   ```
   - 多项目时，后端自动按顺序尝试每个项目的 `openspec/changes/{change_id}/tasks.md`
   - 第一个存在该 change 的项目即命中，无需手动指定「当前项目」
   - 若需固定当前项目，可配 `LANGGRAPH_CURRENT_PROJECT_KEY`（可选）

## 6. 首次验收看什么

重点看：

1. 自己使用的宿主属于“白名单宿主”还是“第三方宿主”
2. `AGENT_HOST_TYPE` 是否传对
3. 决策文件 / 反馈文件是否生成
4. 主 Agent 是否按治理规则回答

如果以上 4 项都正常，说明首次初始化基本通过。  
更详细的内部联调验收资料属于维护资料，默认不作为新用户入口公开依赖。

## 7. 进阶：规则 × SKILL × memory 的协同理解（可选）

- **规则层**：`OpenSpec.md`、`global-rules/*.md`、`agents/*.md`、`skills-rules-for-agent.md`  
  - 只回答「谁/何时/必须做什么」以及极薄的一层结论级 HOW（如日志最小结构、必须写入的路径等）；  
- **技能层**：`skills/*/SKILL.md`  
  - 在具体角色与技能下，描述「怎么一步步做」「产出物最低结构」；  
- **记忆层**：`memory/`  
  - 存放多次迭代中沉淀下来的 patterns / anti-patterns / reflections / playbooks / preferences 等经验性内容。  

> 简单理解：**rules 定义「该不该做、什么时候做、做到什么程度算达标」；SKILL 和 memory 定义「具体怎么做」**。  
> 你可以先不深入记忆这部分，只要知道它们存在即可，后续当关心运行日志、长期记忆或规则演进时再按需查阅。

## 8. 进阶：启用运行日志与长期记忆（可选）

在完成基础初始化之后，你可以按需启用以下进阶能力：

- **运行日志（runtime-logs）**：
  - 目录：`runtime-logs/`  
    - `README.md`：字段说明与宿主适配概要；  
    - `model-calls/`：按日期分片的 JSON Lines 模型调用日志；  
    - `system-events/`：系统事件文本日志。
  - 脚本：`scripts/runtime-logging/append_cursor_model_call.py`  
    - 统一命令行接口，用于向 `model-calls/*.jsonl` 追加一条模型调用记录；  
    - 各宿主可参考对应的 `platform-adapters/<host>/runtime-logging-implementation.md` 在关键节点调用该脚本。

- **长期记忆（memory）**：
  - 目录：`memory/`  
    - `README.md`：长期记忆库说明；  
    - `schema.md`：frontmatter 字段规范；  
    - `patterns/`、`anti-patterns/`、`preferences/`、`playbooks/`、`reflections/`：按类型存放长期记忆条目。
  - 脚本：`scripts/memory/create_memory_entry.py`  
    - 用于按类型创建新的长期记忆条目（含 frontmatter 与正文骨架）；  
    - 主 Agent 在复盘阶段根据 `agents/主Agent.md` 中的规则，判断是否需要调用该脚本沉淀记忆。

> 提示：对于新用户，不必一开始就启用上述能力；当你开始关心「模型调用成本、稳定性」或希望「跨 change-id 复用经验」时，再按各宿主的从 0 初始化手册底部的“进阶能力”小节接入即可。

## 9. 新用户最常见问题

### 9.1 为什么打开后主 Agent 不生效

通常是因为：

- 没打开仓库根目录
- 宿主入口文件没被加载
- 没重启 IDE

### 9.2 为什么 runtime 不执行

通常是因为：

- Python 依赖没装 → 在 `agent_team_project` 下执行 `bash setup-langgraph-env.sh`（见 5.1）完成一键安装与自检
- `AGENT_TEAM_PROJECT_ROOT` 没传
- 第三方宿主忘了传 `AGENT_HOST_TYPE`
- `.env` 没配好

### 9.3 为什么有的宿主子 Agent 不走内置模型

这是设计行为：

- Cursor / VS Code 是白名单宿主，子 Agent 内置优先
- Continue / OpenAI-Codex 是第三方宿主，子 Agent 直接走 API
